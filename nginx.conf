server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # Hỗ trợ Client-side Routing của React Router
        try_files $uri $uri/ /index.html;
    }
}
