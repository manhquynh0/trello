services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: trello-frontend-container
    restart: always
    ports:
      - "5173:80"

#docker compose up -d --build
